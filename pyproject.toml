[project]
name = "io-manager"
version = "0.1.0"
description = "Modular IO Manager for ETL pipelines"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "polars[connectorx]>=1.0.0",
    "duckdb>=1.0.0",
    "pyarrow>=23.0.0",
]
